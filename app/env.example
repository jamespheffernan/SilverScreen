# Expo Public Environment Variables
# These are embedded in the client build and should not contain secrets

# API base URL - point to your deployed server
EXPO_PUBLIC_API_BASE_URL=http://localhost:3001

# Stripe publishable key (test mode)
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_12345
